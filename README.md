### Heartbeat and Gossip Simulation

Run 'run_trials.py' to simulate the heartbeat and gossip in small scale distributed system.
The number of nodes can be changed with variable NODES in 'node.py' for heartbeat and 'gossip_node.py' for gossip